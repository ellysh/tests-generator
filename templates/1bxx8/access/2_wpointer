OBJECT_ACCESS="""
    int i;
    volatile char* trg_ptr = --target;
    volatile const char* src_ptr = source;
    for (i = 0; i <= strlen(source); i++)
    {
        *trg_ptr = *src_ptr;
        trg_ptr++;
        src_ptr++;
    }"""

