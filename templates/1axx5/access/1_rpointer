OBJECT_ACCESS="""
    memcpy(target, &source, sizeof(*target));
    int i;
    char* trg_ptr = target;
    char* src_ptr = &source;
    for (i = 0; i < sizeof(source); i++)
    {
        if (*trg_ptr != *src_ptr)
            return 1;
        trg_ptr++;
        src_ptr++;
    }"""

