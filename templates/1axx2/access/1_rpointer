OBJECT_ACCESS="""
    memcpy(target, source, 5 * sizeof(*target));
    int i;
    int* trg_ptr = target;
    int* src_ptr = &source;
    for (i = 0; i < 10; i++)
    {
        if (*trg_ptr != *src_ptr)
            return 1;
        trg_ptr++;
        src_ptr++;
    }"""

